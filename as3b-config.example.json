{
  "//": "Deployment support policy — copy to as3b-config.json (or point AS3B_CONFIG / --config at a path) and restart. Missing file = everything enabled; a malformed file DISABLES NetBox and BIG-IP apply until fixed. See SUPPORT-POLICY-PLAN.md.",
  "features": {
    "netbox": true,
    "bigipApply": true
  },
  "unsupported": [
    {
      "class": "Service_L4",
      "mode": "hard",
      "reason": "L4 services are handled elsewhere in this estate"
    },
    {
      "class": "Monitor",
      "when": { "monitorType": "sip" },
      "mode": "soft",
      "reason": "SIP monitors are untested here — check with the LB team first"
    }
  ]
}
